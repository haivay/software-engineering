CREATE VIEW view_task5 AS 
SELECT `students`.`lastName`, ROUND(AVG(`marks`.`mark`), 2) AS 'avg_mark'
FROM `students`
JOIN `marks` ON `students`.`id` = `marks`.`keyStudent`
GROUP BY `students`.`lastName`
ORDER BY `avg_mark` DESC
LIMIT 5